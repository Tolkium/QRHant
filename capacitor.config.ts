import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.qrhunt.festival',
  appName: 'QR Hunt',
  webDir: 'dist/qrhunt/browser',
};

export default config;
