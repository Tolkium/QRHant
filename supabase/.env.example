# Supabase CLI / seed script secrets — never commit the real file.
# Daily work: QRHant-Dev. Prod backup: copy supabase/.env.prod (gitignored).
SUPABASE_URL=https://rvtltgrlsmapwonmwsbf.supabase.co
# Paste the **secret** key (sb_secret_…) from Dashboard → Settings → API → Secret keys.
SUPABASE_SERVICE_ROLE_KEY=sb_secret_paste_here
